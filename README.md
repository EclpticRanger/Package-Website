## Installation Guide
- Clone or download the repo
- (Optional) Create a Virtual environment
- Run __pip install -r requirements.txt__ in a terminal in the project directory
- Run app.py
- Open 127.0.0.1:5000 in a browser
